import { index, integer, pgTable, text, timestamp, uniqueIndex, uuid } from "drizzle-orm/pg-core";
import { customerRelations } from "./booking";
import { businesses } from "./tenancy";
export const fidelitySettings=pgTable("fidelity_settings",{businessId:uuid("business_id").primaryKey().references(()=>businesses.id,{onDelete:"cascade"}),spendCents:integer("spend_cents").notNull().default(1000),pointsAward:integer("points_award").notNull().default(1),rewardPoints:integer("reward_points").notNull().default(10),rewardType:text("reward_type").notNull().default("DISCOUNT_EUR"),rewardValue:integer("reward_value").notNull().default(500),createdAt:timestamp("created_at",{withTimezone:true}).notNull().defaultNow(),updatedAt:timestamp("updated_at",{withTimezone:true}).notNull().defaultNow()});
export const fidelityCards=pgTable("fidelity_cards",{id:uuid("id").primaryKey().defaultRandom(),businessId:uuid("business_id").notNull().references(()=>businesses.id,{onDelete:"cascade"}),customerRelationId:uuid("customer_relation_id").notNull().references(()=>customerRelations.id,{onDelete:"cascade"}),cardNumber:text("card_number").notNull(),points:integer("points").notNull().default(0),createdAt:timestamp("created_at",{withTimezone:true}).notNull().defaultNow(),updatedAt:timestamp("updated_at",{withTimezone:true}).notNull().defaultNow()},t=>[uniqueIndex("fidelity_cards_business_customer_unique").on(t.businessId,t.customerRelationId),uniqueIndex("fidelity_cards_number_unique").on(t.cardNumber),index("fidelity_cards_business_idx").on(t.businessId)]);
