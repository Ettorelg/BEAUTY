"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { FormEvent,useState } from "react"; import { authClient } from "@/lib/auth-client";
export function RegisterForm({redirectTo="/onboarding"}:{redirectTo?:string}){const router=useRouter();const[error,setError]=useState<string>();const[pending,setPending]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setError(undefined);const d=new FormData(e.currentTarget);const password=String(d.get("password"));if(password!==String(d.get("passwordConfirm"))){setError("Le password non coincidono.");setPending(false);return;}const result=await authClient.signUp.email({name:String(d.get("name")),phone:String(d.get("phone")),email:String(d.get("email")),password} as never);if(result.error){setError(result.error.message??"Impossibile creare l’account.");setPending(false);return;}router.push(redirectTo);router.refresh();}return <form className="auth-form" onSubmit={submit}><label>Nome e cognome<input name="name" required/></label><label>Numero di cellulare<input name="phone" type="tel" autoComplete="tel" required/></label><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label><label>Ripeti password<input name="passwordConfirm" type="password" minLength={8} required/></label>{error?<p className="form-error">{error}</p>:null}<button className="primary-button" disabled={pending}>{pending?"Creazione…":"Crea account"}</button><p className="form-footer">Hai già un account? <Link href="/account/login">Accedi</Link></p></form>}
