import Image from "next/image"; import Link from "next/link"; import { LoginForm } from "./login-form"; import { SocialAuthButtons } from "../social-auth-buttons";
export default function LoginPage(){return <main className="auth-shell auth-professional"><section className="auth-card"><Image className="auth-brand-logo" src="/brand/alpha-prenota-logo-v1.png" alt="Logo Alpha Prenota" width={420} height={140} priority/><div className="auth-identity-badge">GESTIONALE</div><p className="eyebrow">Alpha Prenota · Titolari e staff</p><h1>Gestisci la tua attività.</h1><p className="muted">Agenda, servizi, clienti e Fidelity in un unico spazio operativo.</p><div className="auth-role-list"><span>Agenda e prenotazioni</span><span>Staff e listino</span><span>Clienti e statistiche</span></div><SocialAuthButtons appleEnabled={Boolean(process.env.APPLE_CLIENT_ID&&process.env.APPLE_CLIENT_SECRET)} callbackURL="/app" googleEnabled={Boolean(process.env.GOOGLE_CLIENT_ID&&process.env.GOOGLE_CLIENT_SECRET)}/><LoginForm/><p className="auth-secondary-link">Sei un cliente? <Link href="/account/login">Vai all’accesso clienti</Link></p></section></main>}
