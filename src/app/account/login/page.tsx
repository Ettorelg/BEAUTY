import Link from "next/link"; import { LoginForm } from "@/app/(auth)/login/login-form"; import { SocialAuthButtons } from "@/app/(auth)/social-auth-buttons";
export default function Page(){return <main className="auth-shell"><section className="auth-card"><p className="eyebrow">Alpha Beauty · Clienti</p><h1>Prenota e ritrova i tuoi saloni.</h1><p className="muted">Accedi per consultare appuntamenti, Fidelity Card e saloni preferiti.</p><SocialAuthButtons appleEnabled={false} callbackURL="/account/phone" googleEnabled={Boolean(process.env.GOOGLE_CLIENT_ID&&process.env.GOOGLE_CLIENT_SECRET)}/><LoginForm redirectTo="/account"/><p className="auth-secondary-link">Lavori in un salone? <Link href="/login">Accedi al gestionale</Link></p></section></main>}
