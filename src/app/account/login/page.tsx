import Image from "next/image"; import Link from "next/link"; import { LoginForm } from "@/app/(auth)/login/login-form"; import { SocialAuthButtons } from "@/app/(auth)/social-auth-buttons";
export default function Page(){return <main className="auth-shell auth-customer"><section className="auth-card"><Image className="auth-brand-logo" src="/brand/alpha-prenota-logo-v1.png" alt="Logo Alpha Prenota" width={420} height={140} priority/><div className="auth-identity-badge">AREA CLIENTI</div><p className="eyebrow">Alpha Prenota · Prenotazioni</p><h1>Prenota il tuo appuntamento.</h1><p className="muted">Accedi dal link o dal QR dell’attività, poi ritrova appuntamenti e Fidelity Card.</p><div className="auth-role-list"><span>Prenotazione online</span><span>Attività già utilizzate</span><span>Fidelity Card</span></div><SocialAuthButtons appleEnabled={false} callbackURL="/account/phone" googleEnabled={Boolean(process.env.GOOGLE_CLIENT_ID&&process.env.GOOGLE_CLIENT_SECRET)}/><LoginForm redirectTo="/account"/><p className="auth-secondary-link">Sei titolare o staff? <Link href="/login">Vai all’accesso gestionale</Link></p></section></main>}
