import Link from "next/link"; import { LoginForm } from "@/app/(auth)/login/login-form"; import { SocialAuthButtons } from "@/app/(auth)/social-auth-buttons";
export default function Page(){return <main className="auth-shell auth-customer"><section className="auth-card"><div className="auth-identity-badge">AREA CLIENTI</div><p className="eyebrow">Alpha Beauty · Prenotazioni</p><h1>Prenota nel tuo salone.</h1><p className="muted">Accedi dal link o dal QR del tuo salone, poi ritrova appuntamenti e Fidelity Card.</p><div className="auth-role-list"><span>Prenotazione online</span><span>Saloni già utilizzati</span><span>Fidelity Card</span></div><SocialAuthButtons appleEnabled={false} callbackURL="/account/phone" googleEnabled={Boolean(process.env.GOOGLE_CLIENT_ID&&process.env.GOOGLE_CLIENT_SECRET)}/><LoginForm redirectTo="/account"/><p className="auth-secondary-link">Sei titolare o staff? <Link href="/login">Vai all’accesso gestionale</Link></p></section></main>}
