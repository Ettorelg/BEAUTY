import Link from "next/link";
export default async function ConfirmationPage({ params, searchParams }: { params: Promise<{ slug: string }>; searchParams: Promise<{ account?: string }> }) { const [{ slug }, query] = await Promise.all([params, searchParams]); const offerAccount = query.account === "1"; return <main className="auth-shell"><section className="auth-card confirmation-card"><p className="confirmation-mark">✓</p><p className="eyebrow">Prenotazione ricevuta</p><h1>A presto.</h1><p className="muted">Il tuo appuntamento è stato registrato.</p>{offerAccount ? <><h2>Vuoi salvare le tue prenotazioni?</h2><p className="muted">Crea un account con la tua email per ritrovare questo salone e i prossimi appuntamenti.</p><Link className="primary-button link-button" href="/account/register">Crea account</Link><Link className="ghost-button link-button" href={`/s/${slug}`}>Continua senza account</Link></> : <Link className="ghost-button link-button" href={`/s/${slug}`}>Torna al salone</Link>}</section></main>; }
