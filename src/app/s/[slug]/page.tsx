import { and,asc,eq } from "drizzle-orm"; import { headers } from "next/headers"; import Link from "next/link"; import { notFound } from "next/navigation"; import { db } from "@/db/client"; import { businesses,serviceCategories,services } from "@/db/schema"; import { auth } from "@/lib/auth"; import { getPublicAvailability } from "@/modules/availability/application/public-availability"; import { createPublicAppointment } from "./actions";
export default async function Page({params,searchParams}:{params:Promise<{slug:string}>;searchParams:Promise<{service?:string;date?:string;staff?:string}>}){const [{slug},q]=await Promise.all([params,searchParams]);const [b]=await db.select().from(businesses).where(eq(businesses.slug,slug)).limit(1);if(!b||!b.active)notFound();const session=await auth.api.getSession({headers:await headers()});const cat=await db.select({id:services.id,name:services.name,description:services.description,duration:services.durationMinutes,price:services.price,category:serviceCategories.name}).from(services).innerJoin(serviceCategories,and(eq(services.categoryId,serviceCategories.id),eq(serviceCategories.businessId,b.id),eq(serviceCategories.active,true))).where(and(eq(services.businessId,b.id),eq(services.active,true),eq(services.onlineBookable,true))).orderBy(asc(serviceCategories.sortOrder),asc(services.name));const selected=cat.find(x=>x.id===q.service);const today=new Intl.DateTimeFormat("en-CA",{timeZone:b.timezone}).format(new Date());const date=/^\d{4}-\d{2}-\d{2}$/.test(q.date??"")?q.date!:today;let slots=selected?await getPublicAvailability({businessId:b.id,serviceId:selected.id,date,durationMinutes:selected.duration,timezone:b.timezone}):[];const staffOptions=[...new Map(slots.map(x=>[x.staffId,x.staffName])).entries()];if(q.staff)slots=slots.filter(x=>x.staffId===q.staff);const grouped=[...new Map(slots.map(x=>[x.localStart,x])).values()];return <main className="public-shell"><header className="public-header"><div><p className="eyebrow">Prenotazione online</p><h1>{b.name}</h1></div><Link className="ghost-button link-button" href="/account/login">Area clienti</Link></header><section className="booking-layout"><div><h2>1. Scegli il servizio</h2><div className="service-grid">{cat.map(x=><Link scroll={false} className={`service-card ${selected?.id===x.id?"selected":""}`} href={`/s/${slug}?service=${x.id}&date=${date}`} key={x.id}><span className="eyebrow">{x.category}</span><h3>{x.name}</h3><p>{x.description||"Trattamento dedicato al tuo benessere."}</p><strong>€ {Number(x.price).toFixed(2)} · {x.duration} min</strong></Link>)}</div></div>{selected?<div className="booking-panel"><h2>2. Scegli quando e con chi</h2><form method="get" className="date-picker"><input type="hidden" name="service" value={selected.id}/><input type="date" name="date" min={today} defaultValue={date}/><select name="staff" defaultValue={q.staff??""}><option value="">Qualsiasi operatore</option>{staffOptions.map(([id,name])=><option value={id} key={id}>{name}</option>)}</select><button className="ghost-button">Mostra disponibilità</button></form>{grouped.length?<form action={createPublicAppointment} className="public-booking-form"><input type="hidden" name="slug" value={slug}/><input type="hidden" name="serviceId" value={selected.id}/><input type="hidden" name="idempotencyKey" value={crypto.randomUUID()}/><div className="slot-grid">{grouped.map((x)=><label className="slot" key={x.localStart}><input type="radio" name="selection" value={`${x.staffId}|${x.localStart}`} required/><span>{x.label}<small>{q.staff?x.staffName:"Più operatori disponibili"}</small></span></label>)}</div><h2>3. I tuoi dati</h2><input name="customerName" defaultValue={session?.user.name??""} placeholder="Nome e cognome" required readOnly={!!session}/><input name="email" type="email" defaultValue={session?.user.email??""} placeholder="Email" required readOnly={!!session}/><input name="phone" type="tel" placeholder="Telefono"/><button className="primary-button">Conferma prenotazione</button></form>:<p className="empty-state">Nessuna disponibilità per questa selezione.</p>}</div>:<aside className="booking-panel empty-prompt"><p>Seleziona un servizio.</p></aside>}</section></main>}
