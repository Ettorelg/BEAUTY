"use client";
import Link from "next/link"; import { useRef,useState } from "react";
const inputStyle={minHeight:46,padding:"10px 12px",border:"1px solid #d8cec8",borderRadius:11,width:"100%"};
export function BookingDetailsForm({action,slug,serviceId,slots,name,email,phone}:{action:(formData:FormData)=>void|Promise<void>;slug:string;serviceId:string;slots:{staffId:string;localStart:string;label:string;operatorsLabel:string}[];name:string;email:string;phone:string}){const[selected,setSelected]=useState("");const[guest,setGuest]=useState(false);const ref=useRef<HTMLFormElement>(null);const logged=Boolean(email);function confirm(){if(logged)ref.current?.requestSubmit();else setGuest(true)}return <form ref={ref} action={action} className="public-booking-form"><input type="hidden" name="slug" value={slug}/><input type="hidden" name="serviceId" value={serviceId}/><input type="hidden" name="idempotencyKey" value={crypto.randomUUID()}/>{logged?<><input type="hidden" name="customerName" value={name}/><input type="hidden" name="email" value={email}/><input type="hidden" name="phone" value={phone}/></>:null}<div className="slot-grid">{slots.map(slot=><label className="slot" key={slot.localStart}><input type="radio" name="selection" value={`${slot.staffId}|${slot.localStart}`} required onChange={()=>{setSelected(slot.localStart);setGuest(false)}}/><span>{slot.label}<small>{slot.operatorsLabel}</small></span></label>)}</div>{selected&&!guest?<section style={{display:"grid",gap:12}}><button type="button" className="primary-button" onClick={confirm}>Conferma prenotazione</button>{!logged?<div className="button-row"><Link className="ghost-button link-button" href="/account/login">Accedi</Link><Link className="ghost-button link-button" href="/account/register">Registrati</Link><button type="button" className="ghost-button" onClick={()=>setGuest(true)}>Continua come ospite</button></div>:<p className="muted">Prenoti come {name}.</p>}</section>:null}{guest?<section style={{display:"grid",gap:12}}><h2>I tuoi dati</h2><input style={inputStyle} name="customerName" placeholder="Nome e cognome" required/><input style={inputStyle} name="email" type="email" placeholder="Email" required/><input style={inputStyle} name="phone" type="tel" placeholder="Telefono" required/><button className="primary-button">Conferma prenotazione</button></section>:null}{!selected?<p className="muted">Seleziona un orario per continuare.</p>:null}</form>}
