"use client";
import { useState } from "react";
export function BookingDetailsForm({action,slug,serviceId,slots,name,email,phone}:{action:(formData:FormData)=>void|Promise<void>;slug:string;serviceId:string;slots:{staffId:string;localStart:string;label:string;operatorsLabel:string}[];name:string;email:string;phone:string}){const[selected,setSelected]=useState("");const logged=Boolean(email);return <form action={action} className="public-booking-form"><input type="hidden" name="slug" value={slug}/><input type="hidden" name="serviceId" value={serviceId}/><input type="hidden" name="idempotencyKey" value={crypto.randomUUID()}/><div className="slot-grid">{slots.map(slot=><label className="slot" key={slot.localStart}><input type="radio" name="selection" value={`${slot.staffId}|${slot.localStart}`} required onChange={()=>setSelected(slot.localStart)}/><span>{slot.label}<small>{slot.operatorsLabel}</small></span></label>)}</div>{selected?<section><h2>3. I tuoi dati</h2>{logged?<p className="muted">Prenoti come {name} ({email}).</p>:null}<input name="customerName" defaultValue={name} placeholder="Nome e cognome" required readOnly={logged}/><input name="email" type="email" defaultValue={email} placeholder="Email" required readOnly={logged}/><input name="phone" type="tel" defaultValue={phone} placeholder="Telefono" required/><button className="primary-button">Conferma prenotazione</button></section>:<p className="muted">Seleziona un orario per continuare.</p>}</form>}
