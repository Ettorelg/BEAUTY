import { redirect } from "next/navigation"; import { eq } from "drizzle-orm"; import { AppNav } from "../app-nav"; import { requireBusinessContext } from "@/lib/business-context"; import { BUSINESS_TYPES,BUSINESS_TYPE_LABELS,MODULE_LABELS,OPTIONAL_MODULES } from "@/lib/business-settings"; import { saveBusinessSettings } from "./actions"; import { db } from "@/db/client"; import { businesses } from "@/db/schema";
export default async function Page({searchParams}:{searchParams:Promise<{saved?:string}>}){const c=await requireBusinessContext();if(c.role!=="OWNER")redirect("/app/agenda");const q=await searchParams;const[b]=await db.select({whatsappRemindersEnabled:businesses.whatsappRemindersEnabled}).from(businesses).where(eq(businesses.id,c.businessId)).limit(1);const whatsappConfigured=Boolean(process.env.WHATSAPP_ACCESS_TOKEN&&process.env.WHATSAPP_PHONE_NUMBER_ID&&process.env.WHATSAPP_REMINDER_TEMPLATE);return <main className="dashboard-shell"><AppNav businessName={c.businessName} role={c.role}/><section className="page-heading"><div><p className="eyebrow">Personalizzazione</p><h1>Tipo di attività e funzioni</h1></div><p className="muted">Mostra solo gli strumenti utili alla tua attività. Puoi cambiare queste scelte in qualsiasi momento.</p></section>{q.saved?<p className="success-message">Configurazione salvata.</p>:null}<section className="panel"><form action={saveBusinessSettings} className="compact-form stacked"><label>Tipo di attività<select name="businessType" defaultValue={c.businessType}>{BUSINESS_TYPES.map(type=><option key={type} value={type}>{BUSINESS_TYPE_LABELS[type]}</option>)}</select></label><fieldset className="compact-form stacked"><legend>Funzioni attive</legend>{OPTIONAL_MODULES.map(module=><label className="checkbox-row" key={module}><input type="checkbox" name={`module_${module}`} defaultChecked={c.modules.includes(module)}/>{MODULE_LABELS[module]}</label>)}</fieldset><details className="service-category"><summary>Notifiche clienti<span>{whatsappConfigured?"WhatsApp disponibile":"Da configurare"}</span></summary><label className="checkbox-row"><input type="checkbox" name="whatsappRemindersEnabled" defaultChecked={b?.whatsappRemindersEnabled??false}/> Invia il promemoria appuntamento anche tramite WhatsApp</label><p className="muted">Il promemoria viene inviato circa 24 ore prima. Se WhatsApp non è disponibile, il sistema usa automaticamente l’email.</p>{!whatsappConfigured?<p className="empty-state">Per attivarlo configura su Railway WHATSAPP_ACCESS_TOKEN, WHATSAPP_PHONE_NUMBER_ID e WHATSAPP_REMINDER_TEMPLATE.</p>:null}</details><p className="muted">Agenda, servizi, clienti e profilo attività restano sempre disponibili. Il magazzino consente prodotti, giacenze e articoli abbinati ai servizi.</p><button className="primary-button">Salva configurazione</button></form></section></main>}
