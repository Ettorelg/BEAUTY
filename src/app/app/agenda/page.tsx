/**
 * The agenda UI is rendered by the segment layout. Keeping this page empty
 * avoids running the legacy "upcoming appointments" query behind the new
 * calendar, while still preserving the /app/agenda route.
 */
export default function AgendaPage() {
  return null;
}
