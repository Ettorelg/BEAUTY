"use server";
import { eq } from "drizzle-orm"; import { revalidatePath } from "next/cache"; import { z } from "zod"; import { db } from "@/db/client"; import { fidelitySettings } from "@/db/schema"; import { requireBusinessContext } from "@/lib/business-context";
export async function saveFidelitySettings(formData:FormData){const context=await requireBusinessContext();if(context.role!=="OWNER")throw Error("Operazione riservata al titolare.");const data=z.object({spendCents:z.coerce.number().int().min(100),pointsAward:z.coerce.number().int().min(1),rewardPoints:z.coerce.number().int().min(1),rewardType:z.enum(["DISCOUNT_EUR","DISCOUNT_PERCENT","FREE_SERVICE"]),rewardValue:z.coerce.number().int().min(0)}).parse(Object.fromEntries(formData));await db.insert(fidelitySettings).values({businessId:context.businessId,...data}).onConflictDoUpdate({target:fidelitySettings.businessId,set:{...data,updatedAt:new Date()}});revalidatePath("/app/fidelity")}
