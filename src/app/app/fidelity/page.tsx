import { eq } from "drizzle-orm"; import { db } from "@/db/client"; import { fidelityCards,fidelitySettings } from "@/db/schema"; import { requireBusinessContext } from "@/lib/business-context"; import { AppNav } from "../app-nav"; import { saveFidelitySettings } from "./actions";
export default async function Page(){const context=await requireBusinessContext();const [settings]=await db.select().from(fidelitySettings).where(eq(fidelitySettings.businessId,context.businessId));const cards=await db.select({id:fidelityCards.id,number:fidelityCards.cardNumber,points:fidelityCards.points}).from(fidelityCards).where(eq(fidelityCards.businessId,context.businessId));return <main className="dashboard-shell"><AppNav businessName={context.businessName} role={context.role}/><section className="page-heading"><div><p className="eyebrow">Clienti</p><h1>Fidelity</h1></div></section><section className="management-grid"><article className="panel"><h2>Regole Fidelity</h2><form action={saveFidelitySettings} className="compact-form stacked"><label>Ogni € spesi<input name="spendCents" type="number" min="1" defaultValue={(settings?.spendCents??1000)/100}/></label><label>Punti assegnati<input name="pointsAward" type="number" min="1" defaultValue={settings?.pointsAward??1}/></label><label>Punti per premio<input name="rewardPoints" type="number" min="1" defaultValue={settings?.rewardPoints??10}/></label><select name="rewardType" defaultValue={settings?.rewardType??"DISCOUNT_EUR"}><option value="DISCOUNT_EUR">Sconto in euro</option><option value="DISCOUNT_PERCENT">Sconto percentuale</option><option value="FREE_SERVICE">Servizio omaggio</option></select><label>Valore premio<input name="rewardValue" type="number" min="0" defaultValue={(settings?.rewardValue??500)/100}/></label><button className="primary-button">Salva regole</button></form></article><article className="panel"><h2>Card attive</h2><p><strong>{cards.length}</strong> clienti con Fidelity Card.</p></article></section></main>}
