"use client";
import { useEffect,useState } from "react";
function key(value:string){const padding="=".repeat((4-value.length%4)%4),raw=atob((value+padding).replace(/-/g,"+").replace(/_/g,"/"));return Uint8Array.from(raw,c=>c.charCodeAt(0));}
export function PushNotificationButton(){const[supported,setSupported]=useState(false),[active,setActive]=useState(false);useEffect(()=>{setSupported("serviceWorker"in navigator&&"PushManager"in window);if("serviceWorker"in navigator)navigator.serviceWorker.ready.then(r=>r.pushManager.getSubscription()).then(s=>setActive(Boolean(s)));},[]);if(!supported)return null;return <button type="button" className="site-install-button" onClick={async()=>{const registration=await navigator.serviceWorker.ready,current=await registration.pushManager.getSubscription();if(current){await fetch("/api/push/subscription",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({endpoint:current.endpoint})});await current.unsubscribe();setActive(false);return;}if(Notification.permission==="denied"){alert("Le notifiche sono bloccate nelle impostazioni del browser.");return;}const publicKey=process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY;if(!publicKey){alert("Notifiche non ancora configurate dall’amministratore.");return;}const subscription=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:key(publicKey)});const response=await fetch("/api/push/subscription",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(subscription)});if(response.status===401){await subscription.unsubscribe();alert("Accedi prima di attivare le notifiche.");return;}setActive(response.ok);}}>{active?"🔔 Notifiche attive":"🔔 Attiva notifiche"}</button>}
