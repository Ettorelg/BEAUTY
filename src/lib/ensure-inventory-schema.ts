import pg from "pg"; let ready:Promise<void>|undefined;
export function ensureInventorySchema(){if(ready)return ready;ready=(async()=>{const c=new pg.Client({connectionString:process.env.DATABASE_URL});await c.connect();try{await c.query(`CREATE TABLE IF NOT EXISTS inventory_products (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), business_id uuid NOT NULL REFERENCES businesses(id) ON DELETE CASCADE, name text NOT NULL, sku text, sale_price numeric(10,2) NOT NULL DEFAULT 0, stock integer NOT NULL DEFAULT 0, minimum_stock integer NOT NULL DEFAULT 0, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now())`);await c.query(`CREATE UNIQUE INDEX IF NOT EXISTS inventory_products_business_sku_unique ON inventory_products(business_id,sku) WHERE sku IS NOT NULL`);await c.query(`CREATE TABLE IF NOT EXISTS inventory_product_services (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), business_id uuid NOT NULL REFERENCES businesses(id) ON DELETE CASCADE, product_id uuid NOT NULL REFERENCES inventory_products(id) ON DELETE CASCADE, service_id uuid NOT NULL REFERENCES services(id) ON DELETE CASCADE, quantity integer NOT NULL DEFAULT 1, UNIQUE(product_id,service_id))`);await c.query(`CREATE TABLE IF NOT EXISTS inventory_movements (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), business_id uuid NOT NULL REFERENCES businesses(id) ON DELETE CASCADE, product_id uuid NOT NULL REFERENCES inventory_products(id) ON DELETE CASCADE, quantity integer NOT NULL, reason text NOT NULL, note text, created_at timestamptz NOT NULL DEFAULT now())`);}finally{await c.end();}})();return ready;}
