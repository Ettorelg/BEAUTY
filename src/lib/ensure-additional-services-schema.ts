import pg from "pg";let ready:Promise<void>|undefined;
export function ensureAdditionalServicesSchema(){if(ready)return ready;ready=(async()=>{const c=new pg.Client({connectionString:process.env.DATABASE_URL});await c.connect();try{await c.query(`CREATE TABLE IF NOT EXISTS appointment_additional_services (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), business_id uuid NOT NULL REFERENCES businesses(id) ON DELETE CASCADE, appointment_id uuid NOT NULL REFERENCES appointments(id) ON DELETE CASCADE, service_id uuid REFERENCES services(id) ON DELETE SET NULL, service_name text NOT NULL, duration_minutes integer NOT NULL, price text NOT NULL, created_at timestamptz NOT NULL DEFAULT now())`);await c.query(`CREATE INDEX IF NOT EXISTS appointment_additional_services_appointment_idx ON appointment_additional_services(appointment_id)`);}finally{await c.end();}})().catch(error=>{ready=undefined;throw error});return ready;}
